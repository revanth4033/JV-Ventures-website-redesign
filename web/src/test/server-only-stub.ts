// Test stub: `server-only` is a guard module that throws if imported into a
// client bundle. Under vitest (plain Node) there's no such boundary, so we alias
// it to this empty module (see vitest.config.ts).
export {}
